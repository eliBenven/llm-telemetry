# server-collector

Optional backend component.

Provides:
- `/ingest` endpoint for browser snippet events
- optional `/r/<code>` redirect for shortlinks with guaranteed attribution

This makes AI attribution more reliable when referrers are missing.
